<%
   regionservers = eval(@nodes).
      select {|node| node[:roles].include? 'hbase-regionserver'}.
      map{|node| node[:hostname] + "." + @domain};
 -%>
<% regionservers.each do |node| -%>
<%= node %>
<% end -%>
